import { PropsType } from "../clubs";

export default function ClubLandingPage(props:PropsType["landing"]) {
    return (
        <div>
            <h1>Club Landing Page</h1>
            <p>This is a placeholder for the club landing page.</p>
        </div>
    );
}